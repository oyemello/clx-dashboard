# Google Cloud Project ID
GOOGLE_PROJECT_ID=your-project-id

# Authentication Strategy (Choose ONE)

# Option 1: Full Service Account JSON content (Preferred for Vercel)
# Remove line breaks and paste the entire JSON string here
# GOOGLE_SERVICE_ACCOUNT_JSON={"type": "service_account", ...}

# Option 2: Path to Service Account Key File (Preferred for Local Dev)
# GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json
